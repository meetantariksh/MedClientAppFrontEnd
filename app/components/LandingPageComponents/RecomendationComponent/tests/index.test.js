// import React from 'react';
// import { shallow } from 'enzyme';

// import RecomendationComponent from '../index';

describe('<RecomendationComponent />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
